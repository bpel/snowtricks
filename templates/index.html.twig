{% extends 'base.html.twig' %}
 {% block body %}
     <div class="my-3 my-md-5">
         <div class="container">
         <div class="page-header">
             <h1 class="page-title">Accueil</h1>
         </div>
             {% if message is defined and message is not empty %}
                 <div class="alert alert-icon alert-primary" role="alert">
                     <i class="far fa-info-circle"></i>{{ message }}
                 </div>
             {% endif %}
             <div class="row row-cards row-deck">
                 {% for trick in tricks %}
                     <div class="col-sm-6 col-xl-3">
                         <div class="card">
                             {% if trick.illustrations is not empty %}
                                 <a href="{{ path('trick_show', {'id': trick.id}) }}">
                                     <img class="card-img-top card-img-snowtricks" src="{{ asset("build/img/")}}{{ trick.illustrations|first.filename }}" alt="no img">
                                 </a>
                             {% else %}
                                 <a href="{{ path('trick_show', {'id': trick.id}) }}">
                                     <img class="card-img-top card-img-snowtricks" src="{{ asset('build/img/No_Image_Available.jpg')}}" alt="{{ trick.nametrick }}">
                                 </a>
                             {% endif %}
                             <div class="card-body d-flex flex-column">
                                 {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                     <div class="row">
                                         <div class="col-md-6">
                                             <h4><a href="{{ path('trick_show', {'id': trick.id}) }}">{{ trick.nameTrick }}</a></h4>
                                         </div>
                                         <div class="col-md-6 text-right">
                                             <a class="btn btn-outline-warning btn-sm" href="{{ path('trick_edit', {'id': trick.id}) }}" role="button"><span class="fas fa-edit"></span></a>
                                             <button class="btn btn-outline-danger btn-sm delete-button-trick" value="{{ trick.id }}"><span class="fas fa-trash"></span></button>
                                         </div>
                                     </div>
                                 {% else %}
                                     <h4><a href="{{ path('trick_show', {'id': trick.id}) }}">{{ trick.nameTrick }}</a></h4>
                                 {% endif %}
                             </div>
                         </div>
                     </div>
                 {% endfor %}
             </div>
         </div>
     </div>
     <div id="delete-modal" class="modal fade bd-example-modal-sm" tabindex="-1" role="dialog">
         <div class="modal-dialog" role="document">
             <div class="modal-content">
                 <div class="modal-header">
                     <h5 class="modal-title">Suppression figure</h5>
                     <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                         <span aria-hidden="true">&times;</span>
                     </button>
                 </div>
                 <div class="modal-body">
                     <p>Voulez vous vraiment supprimer cette figure ?</p>
                 </div>
                 <div class="modal-footer">
                     <a class="btn btn-secondary" role="button" data-dismiss="modal">Annuler</a>
                     <a class="btn btn-danger" href="{{ path('trick_delete', {'id': 1}) }}" role="button">Supprimer</a>
                 </div>
             </div>
         </div>
     </div>
     <script>
         let buttonsDeleteTrick = document.getElementsByClassName("delete-button-trick");
         for (var i = 0; i < buttonsDeleteTrick.length; i++) {
             buttonsDeleteTrick[i].onclick = function()
             {
                 console.log(this.);
                 $('#delete-modal').modal('show');
                 console.log($.value());
             }
         }
     </script>
 {% endblock %}